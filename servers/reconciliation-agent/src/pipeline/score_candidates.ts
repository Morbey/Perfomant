import { Candidate, MatchingPrefs } from '../types';

export function scoreCandidates(candidates: Candidate[], prefs: MatchingPrefs): Candidate[] {
    // TODO: Implement candidate scoring
    return candidates;
}
